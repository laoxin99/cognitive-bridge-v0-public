# CognitiveBridge-v0 公开边界

## 允许公开的表述

```text
这是一个可运行的离线机制演示。
外部思想流可以进入结构化候选层。
候选经过 schema、边界、账本和低权限承接检查。
固定 fixture 能够展示反馈驱动的路径改道与再次验收。
事件记录和 replay hash 可以复核该固定演示链。
```

## 不允许公开的表述

```text
已经实现 AGI
已经实现机器意识或完整类人智慧
已经解决通用 AI 安全问题
能够控制所有真实智能体
能够保证生产环境绝对不失控
LLM 已经接入真实高权限执行系统
V14 已经作为完整生产级安全底座公开
```

## 技术边界

```text
offline fixture only
no real LLM provider
no network or API key
no V14 main-chain import
no ReactionCore modification
no SystemState write
no external action execution
no autonomous final-goal setting
```

## Replay 语义

当前 replay 表示：

```text
同一固定 fixture 可产生相同事件链和 hash；
事件顺序、选择结果和验收结果可复核；
未同步修改 ledger 时可以发现摘要不一致。
```

它不表示：

```text
带密钥签名的不可篡改账本
不可删除的 append-only 审计基础设施
任意历史运行的完整重构
生产级审计能力
```

## 许可证与复用政策

```text
本候选包当前未包含 LICENSE 文件。
在所有者明确选择许可证之前，不授予源码复用、修改、再发布或商业使用许可。
正式发布前必须由所有者选择已批准的许可证，或明确标注 NO LICENSE / ALL RIGHTS RESERVED。
“公开查看”不等于“开源授权”。
```
